<% content_for :title, "Editing appointment" %>
<div class="container mt-4">
  <h1>Make changes to appointment</h1>

  <%= render "form", appointment: @appointment %>
  <div class="mt-3">
    <%= link_to "Show this appointment", @appointment, class: "btn btn-outline-secondary btn-sm" %> |
    <%= link_to "Back to appointments", appointments_path, class: "btn btn-outline-secondary btn-sm"  %>
  </div>
</div>
